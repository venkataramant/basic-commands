# Streams API
	Arrays.asList().stream()
	Arrays.stream()
	list.stream()
	
	Methods
		filter(Predicate)
		allMatch(Predicate) [Return True/False]
		anyMatch(Predicate)
		noneMatch(Predicate)
		ifPresent(lambda)
		
		sort(Comparator.comparing(Person::getAge).thenComparing(Person::getGender).reversed())
		
		forEach()
	
		collect(Collectors.toList())
		
		max(Predicate)
		min(Predicate)
		count(Predicate)
		
		 

# Filter
# ForEach
# Collect

